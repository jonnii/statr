Entries:
- Pattern: ^stats
  Retentions:
  - 2s:10d

- Pattern: ^immedate.bucket
  Storage:
    Type: ImmediateStorageStrategy

- Pattern: ^buffered.with.properties
  Storage:
    Type: BufferedStorageStrategy
    Properties:
      BufferSize: 10000