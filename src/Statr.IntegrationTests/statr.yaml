Directory: c:\dev\tmp\storage
Namespace: integration

Entries:
- Pattern: ^stats
  Retentions:
  - 2s:10d

- Pattern: ^immedate.bucket
  Buffer:
    Type: ImmediateStrategy

- Pattern: ^buffered.with.properties
  Buffer:
    Type: BufferedStrategy
    Properties:
      BufferSize: 10000